<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="UrGuide.Mobile.Views.Templates.Profile.PostsTemplate"
             xmlns:st="clr-namespace:Plugin.SharedTransitions;assembly=Plugin.SharedTransitions"
             xmlns:vm="clr-namespace:UrGuide.Mobile.ViewModels"
             xmlns:models="clr-namespace:UrGuide.Mobile.Models"
             xmlns:renderedViews="clr-namespace:Sharpnado.Presentation.Forms.RenderedViews;assembly=Sharpnado.Presentation.Forms" xmlns:loader="clr-namespace:Sharpnado.Presentation.Forms.CustomViews;assembly=Sharpnado.TaskLoaderView">
    <loader:TaskLoaderView TaskLoaderNotifier="{Binding PostsLoader, Source={RelativeSource AncestorType={x:Type vm:ProfileViewModel}}}">
        <renderedViews:HorizontalListView
                                        Margin="0,20"
                                        CollectionPadding="0,8"
                                        InfiniteListLoader="{Binding PostsPaginator, Source={RelativeSource AncestorType={x:Type vm:ProfileViewModel}} }"
                                        ItemSpacing="8"
                                        ItemHeight="230"
                                        ColumnCount="1"
                                        HorizontalOptions="FillAndExpand"
                                        ItemsSource="{Binding Posts, Source={RelativeSource AncestorType={x:Type vm:ProfileViewModel}}}"
                                        ListLayout="Grid"
                                        VisibleCellCount="4"
                                                      >

            <renderedViews:HorizontalListView.ItemTemplate>

                <DataTemplate x:DataType="{x:Type models:PostItem}">
                    <ViewCell>
                        <Frame VerticalOptions="FillAndExpand" HorizontalOptions="FillAndExpand"
                               CornerRadius="4" BorderColor="{StaticResource IconColorLight}">
                            <Grid RowDefinitions="70,Auto,*" ColumnDefinitions="*" 
                              >
                                <StackLayout VerticalOptions="StartAndExpand" Orientation="Vertical" Spacing="12"
                                         HorizontalOptions="FillAndExpand" Grid.ColumnSpan="2"
                            st:Transition.Name="Author" st:Transition.Group="{Binding Id}">
                                    <Label Text="{Binding Author}" FontSize="Medium" 
                       FontAttributes="Bold"/>

                                    <Label FontSize="Small" Text="{Binding PublicationDate}">

                                    </Label>
                                    <Label FontSize="Small" Text="{Binding Countdown.Left}"
                                       HorizontalOptions="EndAndExpand">
                                    </Label>
                                </StackLayout>
                                <!--Categories-->
                                <StackLayout Grid.Row="1" VerticalOptions="StartAndExpand" 
                             Orientation="Horizontal" Spacing="8"
                             BindableLayout.ItemsSource="{Binding Categories}"
                             st:Transition.Group="{Binding Id}"
                             st:Transition.Name="Categories"
                             Margin="0,8,0,0">
                                    <BindableLayout.ItemTemplate>
                                        <DataTemplate x:DataType="{x:Type x:String}">
                                            <Frame CornerRadius="4" Padding="8" HasShadow="True" BackgroundColor="{StaticResource IconColor}">
                                                <Label Text="{Binding}" FontSize="Small" FontAttributes="Bold" 
                                           TextColor="White" MinimumWidthRequest="70"/>
                                            </Frame>
                                        </DataTemplate>
                                    </BindableLayout.ItemTemplate>
                                </StackLayout>
                                <!--Images-->
                                <StackLayout Grid.Row="2" Orientation="Vertical" Grid.ColumnSpan="2"
                             VerticalOptions="StartAndExpand" Spacing="8">
                                    <Label Text="{Binding Description}" 
                           VerticalOptions="Start" HorizontalOptions="StartAndExpand"
                                               LineBreakMode="TailTruncation"
                                               MaxLines="3"/>

                                </StackLayout>
                            </Grid>
                            <Frame.GestureRecognizers>
                                <TapGestureRecognizer 
                                    Command="{Binding Source={RelativeSource AncestorType={x:Type vm:MainPageViewModel}}, Path=Posts.ViewDetailsCommand}"
                                    CommandParameter="{Binding}"
                                    NumberOfTapsRequired="1"
                                    />
                            </Frame.GestureRecognizers>
                        </Frame>

                    </ViewCell>
                </DataTemplate>
            </renderedViews:HorizontalListView.ItemTemplate>
        </renderedViews:HorizontalListView>
    </loader:TaskLoaderView>

</ContentView>