<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
             xmlns:ff="clr-namespace:FFImageLoading.Forms;assembly=FFImageLoading.Forms"
             x:Class="UrGuide.Mobile.Views.Controls.Avatar" BackgroundColor="Transparent"
             x:Name="page" HeightRequest="46" WidthRequest="46">
    <ff:CachedImage x:Name="image" 
                    Aspect="AspectFit" 
                    HorizontalOptions="FillAndExpand"
                    VerticalOptions="FillAndExpand"
                    WidthRequest="{Binding Source={Reference page}, Path=WidthRequest}"
                    HeightRequest="{Binding Source={Reference page}, Path=HeigthRequest}"
                    >
        <ff:CachedImage.GestureRecognizers>
            <TapGestureRecognizer Tapped="TapGestureRecognizer_Tapped">
                
            </TapGestureRecognizer>
        </ff:CachedImage.GestureRecognizers>
    </ff:CachedImage>
</ContentView>